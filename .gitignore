# Secrets — never auto-commit these
.env
.env.*
*.pem
*.key
*.p12
credentials*.json
service-account*.json
secrets*.json

# Local Claude Code overrides (personal, per-session)
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
